@page
@model CyberSecurityTraining.Areas.Admin.Pages.Modules.IndexModel
@{
    ViewData["Title"] = "Training Modules";
}

<div class="container-fluid">
    <!-- Success/Error Messages -->
    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="fas fa-check-circle"></i> @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }
    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <i class="fas fa-exclamation-circle"></i> @TempData["ErrorMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <div>
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb mb-2">
                    <li class="breadcrumb-item"><a asp-area="Admin" asp-page="/Dashboard">Dashboard</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Training Modules</li>
                </ol>
            </nav>
            <h1 class="h3 mb-0 text-gray-800">Training Modules</h1>
        </div>
        <a asp-area="Admin" asp-page="/Modules/Create" class="btn btn-primary">
            <i class="fas fa-plus"></i> Add New Module
        </a>
    </div>

    <div class="card shadow mb-4">
        <div class="card-header py-3">
            <h6 class="m-0 font-weight-bold text-primary">All Training Modules</h6>
        </div>
        <div class="card-body">
            @if (Model.Modules.Any())
            {
                <div class="table-responsive">
                    <table class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                <th>Order</th>
                                <th>Title</th>
                                <th>Description</th>
                                <th>Lessons</th>
                                <th>Status</th>
                                <th>Created</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var module in Model.Modules)
                            {
                                <tr>
                                    <td>@module.Order</td>
                                    <td>@module.Title</td>
                                    <td>@(module.Description ?? "No description")</td>
                                    <td>@module.Lessons.Count</td>
                                    <td>
                                        @if (module.IsActive)
                                        {
                                            <span class="badge bg-success">Active</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-secondary">Inactive</span>
                                        }
                                    </td>
                                    <td>@module.DateCreated.ToString("MMM dd, yyyy")</td>
                                    <td>
                                        <a asp-area="Admin" asp-page="/Modules/Details" asp-route-id="@module.Id" class="btn btn-outline-secondary btn-sm">
                                            <i class="fas fa-eye"></i>
                                        </a>
                                        <a asp-area="Admin" asp-page="/Modules/Edit" asp-route-id="@module.Id" class="btn btn-outline-secondary btn-sm">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                        <a asp-area="Admin" asp-page="/Modules/Delete" asp-route-id="@module.Id" class="btn btn-outline-secondary btn-sm">
                                            <i class="fas fa-trash"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="text-center py-5">
                    <h5>No Training Modules Found</h5>
                    <p class="text-muted">Get started by creating your first training module.</p>
                    <a asp-area="Admin" asp-page="/Modules/Create" class="btn btn-primary">
                        <i class="fas fa-plus"></i> Create Module
                    </a>
                </div>
            }
        </div>
    </div>
</div>
